#include<stdio.h>
int main(void)
{
   /* int x,y,z;
    x = 8++;
    y = ++x++;
    z = (x+y)--;
    printf("x=%d, y=%d, z=%d\n",x,y);
    return 0;
    */
   //this output will be ERROR because  increment/decrement dosen't work with constent and expression
}